# Keriet Farm

1. Copy `.env.example` and rename the copy to `.env`.
2. Run `npm install`.
3. Run `npm run dev`.
4. Open `http://localhost:3000`.

The avocado price is set to KSh 30 each because no final avocado price was provided.
